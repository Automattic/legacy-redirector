{
  "name": "wpcom-legacy-redirector",
  "version": "1.0.0",
  "description": "WordPress plugin for handling large volumes of legacy redirects in a scalable manner.",
  "private": true,
  "scripts": {
    "wp-env": "wp-env"
  },
  "devDependencies": {
    "@wordpress/env": "^11.10.0"
  }
}
